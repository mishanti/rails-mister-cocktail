<h1>Add a new cocktail</h1>

<div class="container">
  <%= simple_form_for([@cocktail]) do |f| %>
    <%= f.input :name %>
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
    <%= f.submit %>
  <% end %>
</div>
